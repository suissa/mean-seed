section.pure-g
  article.pure-u-1-3
  article.pure-u-1-3
    h3.title List of Applications
    p.message {{ message }}

    table
      thead
        tr
          th 
      tbody
        tr(data-ng-repeat='application in applications') 
          td
            {{application.id}}
          td
            a(href='/applications/{{application._id}}') 
              | {{application.name}}
          td
            a(href='/applications/{{application._id}}/edit') 
              | EDIT
          td 
            span(data-ng-controller='RemoveApplication')
              button(data-ng-click='remove(application)') DELETE
        
  article.pure-u-1-3

